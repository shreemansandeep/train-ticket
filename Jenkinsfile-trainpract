pipeline {
    agent any

    stages {
        stage('Git Checkout') {
            steps {
                git branch: 'main', url: 'https://github.com/shreemansandeep/train-ticket.git'
            }
        }

        /* Commented out host-based Maven stages
        stage('Unit Testing') {
            steps {
                sh 'mvn test'
            }
        }

        stage('Maven Build') {
            steps {
                sh 'mvn clean package'
            }
        }
        
        stage('SonarQube Analysis') {
            steps {
                script {

                    // Fetch Sonar Token from AWS Secrets Manager
                    def sonarToken = sh(
                        script: """
                            aws secretsmanager get-secret-value \
                            --secret-id test/train/sonarid \
                            --region ap-south-1 \
                            --query SecretString \
                            --output text | jq -r '.SONAR_TOKEN'
                        """,
                        returnStdout: true
                    ).trim()

                    sh """
                        mvn clean verify org.sonarsource.scanner.maven:sonar-maven-plugin:sonar \
                          -Dsonar.projectKey=Train-Ticket-System \
                          -Dsonar.host.url=http://43.205.235.159:9000 \
                          -Dsonar.login=${sonarToken} \
                          -Dsonar.token=${sonarToken}
                    """
                }
            }
        }
        */

        // Maven Build Agents using Docker Containers (via Docker CLI)
        stage('Unit Testing in Container') {
            steps {
                sh 'docker run --rm -v $HOME/.m2:/root/.m2 -v ${WORKSPACE}:/app -w /app maven:3.8.6-openjdk-11 mvn test'
            }
        }

        stage('Maven Build in Container') {
            steps {
                sh 'docker run --rm -v $HOME/.m2:/root/.m2 -v ${WORKSPACE}:/app -w /app maven:3.8.6-openjdk-11 mvn clean package'
            }
        }
    }
}

